# AI 서비스 개발 공부 커리큘럼

> 학생: 창희 / 선생: Claude (Claude Code)
> 시작: 2026-07-30 / 목표 기간: 약 3개월 (12주)
> 이 파일은 커리큘럼의 기준 문서다. 새 세션에서 이어갈 때 Claude에게 "CURRICULUM.md랑 PROGRESS.md 읽고 공부 이어가자"라고 하면 된다.

## 학생 프로필

- 직군: 서버 인프라 (Windows Server, Ubuntu, Samba, MySQL 복제, keepalived HA, 백업/DR 구축 경험)
- 파이썬: 기초 문법 수준 (혼자 프로그램 작성 경험 적음)
- 투자 시간: 주 10시간 이상 (개념 2h + 실습 7h + 복습 1h)
- 최종 목표: LLM API·RAG·에이전트로 실제 동작하는 AI 서비스를 만들 수 있게 되기

## 교재(모델 환경) 선택

- **Ollama + 로컬 오픈소스 모델** (사용자 결정, 2026-07-30)
- 기본 모델: `qwen2.5:7b` (한국어 우수, 4.7GB) / 학습 PC: i7-13620H, RAM 16GB, RTX 4060 Laptop(VRAM 8GB)
- 이유: 완전 무료·무제한, API 키 불필요, **데이터가 밖으로 안 나감**(회사 문서도 안전),
  인프라 배경과 잘 맞음. 호출 패턴(클라이언트→모델→메시지→응답)은 상용 API와 동일해서 이식 쉬움
- 트레이드오프: 상용 최상위 모델보다 품질 낮음. 구조화 출력·Tool Use(4~5주차)에서 한계를 느끼면
  그때 상용 API(Gemini 무료 티어 등)와 비교 실습 — 이것도 학습 포인트
- 참고: venv에 anthropic·google-genai 패키지도 설치돼 있어 언제든 비교 가능

## 진행 방식 (규칙)

1. 매주 과제를 받고 **코드는 직접 작성**한다. 복붙 금지, 한 줄씩 이해하며 작성
2. 작성한 코드는 Claude에게 보여주고 리뷰받는다. Claude는 답 대신 힌트를 먼저 준다
3. 에러가 나면 에러 메시지를 먼저 스스로 읽는다 (에러 읽기도 커리큘럼의 일부)
4. 주차가 끝나면 해당 주차 디렉토리(`weekNN/`)에 코드와 배운 것 메모를 커밋한다
5. 이론(시스템 디자인)은 따로 파지 않고, 각 단계 끝의 "설계 코너"에서 만든 것과 연결해서 배운다

---

## 1단계 (1~2주차): 파이썬 근육 + 첫 API 호출

### 1주차 — 환경 세팅과 첫 호출
- venv, pip, 환경변수 개념
- Gemini API 키 발급, `hello.py` 작성 (클라이언트 생성 → 모델 지정 → 메시지 전송 → 응답 출력)
- 확장: `response.usage_metadata`로 토큰 수 확인, 비용 계산 개념
- 체크포인트: "API 키를 코드에 하드코딩하면 안 되는 이유를 설명할 수 있다"

### 2주차 — 문서 요약 CLI
- 파일 읽기(open/pathlib), 함수 분리, sys.argv 또는 argparse
- 결과물: `summarize.py 파일명` 실행하면 파일 내용을 요약해주는 CLI
- 실험: 시스템 프롬프트(system instruction)로 말투/형식 바꿔보기
- 체크포인트: "프롬프트와 시스템 프롬프트의 차이를 설명할 수 있다"

## 2단계 (3~6주차): LLM 앱 핵심 패턴 3종

### 3주차 — 대화와 스트리밍
- 대화 이력(messages 배열) 관리, 멀티턴 챗봇 CLI
- 스트리밍 출력 (한 글자씩 실시간 표시)
- 체크포인트: "API는 상태가 없다(stateless)는 말의 의미를 설명할 수 있다"

### 4주차 — 구조화된 출력 (JSON)
- 모델 응답을 JSON 스키마로 강제하기, 파이썬 dict로 파싱
- 연습: 아무 글에서나 "요약 / 키워드 / 감정"을 JSON으로 추출
- 체크포인트: "왜 자유 텍스트가 아니라 JSON으로 받아야 하는가"

### 5주차 — 함수 호출 (Tool Use)
- 모델이 내 파이썬 함수를 호출하게 만들기 (예: 날씨 함수, 계산 함수)
- 체크포인트: "모델이 함수를 '실행'하는 게 아니라 '요청'한다는 것을 설명할 수 있다"

### 6주차 — 마일스톤 1: MeetingAssistant 업그레이드
- 기존 meeting_listen.py의 전사 결과를 LLM에 넣어 "요약 + 결정사항 + 할 일" 자동 추출
- 2~5주차 배운 것 전부 사용 (파일 입출력 + JSON 출력 + 프롬프트 설계)
- **설계 코너**: API 호출이 실패하면? — 재시도, 타임아웃, 에러 처리 설계

## 3단계 (7~9주차): RAG (문서 검색 기반 답변)

### 7주차 — 임베딩 개념
- 임베딩 = 텍스트를 숫자 벡터로. 유사도 검색의 원리
- chromadb 설치, 문장 몇 개 넣고 유사한 문장 찾기 실험
- 체크포인트: "키워드 검색과 벡터 검색의 차이를 설명할 수 있다"

### 8주차 — 문서 인덱싱
- PDF/docx 텍스트 추출, 청킹(문서 쪼개기) 전략
- 문서 여러 개를 chromadb에 넣고 질문으로 관련 조각 검색
- 체크포인트: "청크가 너무 크면/작으면 각각 무슨 문제가 생기는가"

### 9주차 — 마일스톤 2: 문서 QA 봇
- 질문 → 관련 문서 조각 검색 → 모델에게 조각과 함께 질문 → 근거 있는 답변
- **데이터 프라이버시 결정**: 회사 문서를 쓸 거면 유료 API 또는 사내 서버 Ollama로 전환
- **설계 코너**: 문서가 1만 개면? — 인덱스, 검색 품질 평가, 청킹 트레이드오프

## 4단계 (10~12주차): 에이전트화 — 나만의 작업 위임 비서
> 2026-08-01 개편: 원래 서비스화(FastAPI→웹UI→사내배포)였으나, 사용자 목표가
> "개인용 + 로컬 LLM에 작업 위임(AgentOS)"으로 확정되어 에이전트 트랙으로 변경.
> 서비스화 트랙은 아래 "보류 트랙"으로 이동 — 사내 수요가 생기면 부활.

### 10주차 — 도구 쓰는 챗봇 (5주차 보너스의 완성)
- 3주차 chatbot.py(루프+이력) + 5주차 toolbot.py(도구) 합체
- 대화하다가 필요하면 도구를 쓰고, 결과를 기억하는 봇
- 체크포인트: 도구 결과가 대화 이력에 어떻게 쌓이는지 설명할 수 있다

### 11주차 — 도구 확장: 내 비서 만들기
- 실용 도구 추가: 폴더 목록/파일 검색, 시스템 상태, 규정 검색(week08 인덱스를 도구로!),
  회의록 요약(week06을 도구로!) — 기존 결과물이 전부 에이전트의 손발이 됨
- 위험한 도구(파일 이동/삭제)는 실행 전 사용자 확인(y/n) — 안전장치 설계
- 체크포인트: 왜 삭제 도구엔 확인이 필요하고 조회 도구엔 불필요한가

### 12주차 — 마일스톤 3: 멀티스텝 에이전트
- 모델이 완료까지 도구를 연속 호출하는 루프 (도구 결과를 보고 다음 도구를 스스로 결정)
- 최대 반복 횟수 제한(폭주 방지), 중간 경과 출력
- 목표: "다운로드 폴더 정리해줘" 수준의 작업 위임이 되는 미니 에이전트
- **설계 코너**: 에이전트가 폭주하면? — 반복 한도, 도구 권한, 로그

## 이후 (선택 심화)

- 에이전트 고도화: 작업 스케줄러 연동(정기 실행), 메모리 파일, 더 큰 모델 비교
- **보류 트랙 — 서비스화**: FastAPI로 HTTP API화 → 웹 UI → 사내 우분투 서버 배포(systemd).
  동료와 공유할 실수요가 생기면 시작 (인프라 경험 덕에 반나절거리)
- Ollama/vLLM으로 로컬 모델 운영 (AI 인프라 방향)
- 시스템 디자인 독서: 『가상 면접 사례로 배우는 대규모 시스템 설계 기초』 — 만들어본 뒤에 읽어야 흡수됨

---

## 세션 이어가기 (Claude용 메모)

- 진행 상태는 `PROGRESS.md`에 체크리스트로 기록한다. 주차 완료 시 Claude가 갱신한다
- 각 주차 코드는 `weekNN/` 디렉토리에, 배운 점 메모는 `weekNN/NOTES.md`에 남긴다
- 선생 역할 원칙: 답 대신 힌트 먼저, 코드 리뷰 필수, "왜"를 항상 물을 것
