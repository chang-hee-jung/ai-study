# 17주차 정리

## 1단계 — 붙이기 전에 모델부터 쟀다

Hermes Agent는 도구 호출 위에 서 있는 물건이라, 붙이기 전에 우리 모델이
도구 호출을 되는지부터 확정했다. 결과가 예상보다 컸다.

### 측정 결과

4문항: 기본 호출 / 한글 인자 보존 / 도구 선택 / 과잉 호출 억제

| 모델 | 점수 | 시간 | 걸린 지점 |
|---|---|---|---|
| **gemma4:e4b-it-qat** | **4/4** | 29.5초 | 없음 |
| qwen3.5:9b | 4/4 | 36.4초 | 없음 |
| qwen2.5:7b | 3/4 | 16.7초 | `서울` → `Seoul`로 번역해서 넘김 |
| llama3.1:8b | 2/4 | 10.5초 | 경로를 `/week17/`로 뭉갬 + 불필요한 도구 호출 |
| qwen3:8b | — | — | **설치되어 있지 않음 (404)** |

### 건진 것 1 — agent.py가 지금 안 돈다

`week12/agent.py`의 `BASE_MODEL = "qwen3:8b"` 인데 이 모델이 이 PC에 없다.
13주차에 `qwen2.5 7b/14b → qwen3 8b/14b`로 세대 교체했고, 그 뒤 `qwen3.5:9b`가
들어오면서 `qwen3:8b`가 정리된 것으로 보인다. 코드는 따라가지 않았다.

**모델 이름을 코드에 상수로 박아두면 이렇게 조용히 썩는다.** 모델을 지우거나
바꿀 때 코드가 아무 말도 하지 않는다. 실행해봐야 404를 본다.

Hermes가 `config.yaml`로 모델을 빼둔 이유가 이것이다. 설정과 코드를 분리하면
모델 교체가 코드 수정이 아니라 설정 변경이 된다. 3단계에서 이 부분을 자세히 본다.

### 건진 것 2 — gemma4:e4b가 도구 호출도 1등

15주차에 문서QA로 뽑았던 그 모델이 도구 호출에서도 만점이다.
VRAM 2.87GB로 제일 가벼운데 제일 정확하다. 에이전트용 모델을 따로 둘 이유가 없어졌다.

13주차에 짰던 "손발 7B / 참모 14B" 이원화는 이제 근거가 약하다.

### 건진 것 3 — 12주차 중국어 사고와 같은 계열의 버그를 다시 봤다

`qwen2.5:7b`가 "서울"을 `Seoul`로 **번역해서** 도구에 넘겼다.
12주차에 폴더 이름을 `文档`으로 바꿔버린 그 사고와 같은 뿌리다.
모델은 인자를 "전달"하는 게 아니라 "생성"하기 때문에, 원문 보존을 지시하지
않으면 자기 판단으로 바꾼다.

`agent.py`의 SYSTEM 프롬프트에 "한 글자도 바꾸지 말고 그대로 사용한다"가 들어간
이유가 이것이다. 이번 측정은 프롬프트 없이 맨몸으로 재서 그 차이가 드러났다.

### 남는 질문

`qwen3:14b`, `qwen2.5:14b`, `gemma4:12b-it-qat`는 시간이 오래 걸려 아직 안 쟀다.
필요하면 `python week17/tool_check.py qwen3:14b` 로 개별 측정한다.

## 2단계 — 설치와 연결

설치는 `-SkipSetup`으로 붙였고, `config.yaml`에 Ollama를 openai-compatible
provider로 꽂아 바로 돌았다. 첫 응답 13초.

### 첫 실행에서 경고가 떴다

```
⚠️ Context file AGENTS.md TRUNCATED: 74668 chars exceeds limit of 31457
```

**내 예상은 틀렸다.** 나는 Ollama 기본 컨텍스트(4096)가 좁아서 터질 거라고
예고했는데, 그게 아니었다.

진짜 원인은 **실행한 폴더**였다. 설치 소스 폴더
`AppData\Local\hermes\hermes-agent` 안에서 `hermes chat`을 실행했고,
그 폴더에 `AGENTS.md`가 있었다. 열어보니 이런 파일이다.

```
# Hermes Agent - Development Guide
Instructions for AI coding assistants and developers working on the hermes-agent codebase.
```

에이전트의 시스템 프롬프트가 아니라 **그 저장소를 고치는 AI에게 주는 개발자
가이드**다. Hermes는 현재 폴더의 `AGENTS.md`를 프로젝트 컨텍스트로 자동
로드하는데, 하필 자기 소스 폴더에서 실행하니 자기 개발 문서 76KB를 통째로
집어삼킨 것이다.

다른 폴더에서 실행해보니 경고가 사라졌다. 원인 확정.

### 여기서 배운 것

**에이전트는 "지금 어느 폴더에 있는가"를 입력으로 삼는다.**

내 `agent.py`에는 그 개념이 아예 없다. 어디서 실행하든 똑같이 동작한다.
`week12/testbed`를 경로 문자열로 직접 받을 뿐이다.

Hermes는 실행 위치에서 프로젝트 파일을 읽어 그 프로젝트에 맞게 행동한다.
편리한 만큼, 오늘처럼 의도치 않은 파일을 빨아들이기도 한다. 기능이자 함정이다.

그리고 경고 문구가 좋았다. 잘렸다는 사실 + 한계값 + **해결책 3개**를 같이 줬다.
내 agent.py는 뭔가 잘못되면 그냥 에러를 뱉거나 조용히 이상하게 동작한다.

## 3단계 — 해부

`AppData\Local\hermes\` 안을 열어 `week12/agent.py`와 대조했다.

| Hermes | 크기/내용 | 내 agent.py의 어디 |
|---|---|---|
| `SOUL.md` | 513 바이트 | `SYSTEM` 프롬프트 |
| `config.yaml` | 1,708줄 / 23개 최상위 키 | 코드에 박힌 상수들 |
| `state.db` | SQLite + FTS5 + 트라이그램 | `memory.json` |
| `skills/` | 파일 546개 | **없음** |
| `sessions/` + DB `sessions` 테이블 | 대화 단위 관리 | **없음** |
| `logs/`, `hooks/`, `cron/`, `sandboxes/` | | **없음** |
| `.env` | 24KB (열지 않음) | **없음** |

### 반전 — 내 프롬프트가 4배 크다

| | 크기 |
|---|---|
| Hermes `SOUL.md` | 513 바이트 |
| 내 `SYSTEM` 프롬프트 | **1,972 바이트** |

프레임워크가 더 거대한 프롬프트를 쓸 거라 생각했는데 반대였다.

이유는 **규칙을 어디에 두느냐**의 차이다. 내 agent.py는 "한 글자도 바꾸지
말라", "에러 나면 반복하지 말라", "보고 전에 검증하라" 같은 규칙을 전부
프롬프트에 글로 적었다. 모델이 그 글을 매번 읽고 지켜주기를 기대한다.

Hermes는 같은 것들을 **코드와 설정으로 강제한다.** 프롬프트는 정체성만
짧게 주고, 나머지는 지키지 않을 수 없게 만들어둔다.

프롬프트로 부탁한 규칙은 모델이 안 지키면 그만이다. 코드로 막은 규칙은
모델이 어길 수가 없다. 12주차에 중국어 폴더명 사고를 프롬프트 보강과
**도구 방어설계** 둘로 수습했는데, 그때 이미 이 차이를 겪었다.

### tool_loop_guardrails — 같은 발상에 독립적으로 도달했다

```yaml
tool_loop_guardrails:
  warnings_enabled: true
  hard_stop_enabled: false
  warn_after:
    exact_failure: 2
    same_tool_failure: 3
    idempotent_no_progress: 2
  hard_stop_after:
    exact_failure: 5
    same_tool_failure: 8
    idempotent_no_progress: 5
```

내 agent.py에도 있다. `MAX_ROUNDS = 8`, 그리고 같은 도구를 같은 인자로
불렀는데 또 에러나면 감지하는 `last_error_key`.

**발상이 같다.** 다만 정교함이 다르다.

- 내 것: 라운드 상한 1개 + 에러 반복 1종
- Hermes: 실패 유형 3종(완전 동일 실패 / 같은 도구 실패 / 진전 없는 반복) ×
  경고·중단 2단계

그리고 기본값이 `hard_stop_enabled: false`다. **기본은 멈추지 않고 경고만 한다.**
내 것은 8라운드에서 무조건 끊는다. 어느 쪽이 맞는지는 용도에 달렸지만,
"멈출지 말지를 설정으로 뺐다"는 게 핵심이다.

### compression — 내게 없는 것

```yaml
compression:
  enabled: true   # 자동 컨텍스트 압축
```

내 `save_memory()`는 대화 전체를 통째로 쌓는다. 오래 쓰면 `memory.json`이
계속 커지고 결국 컨텍스트 창을 넘겨 터진다. 아직 안 터진 건 오래 안 써봐서다.

Hermes는 이걸 자동 압축으로 푼다. **4단계에서 이걸 훔친다.**

### state.db — 기억을 어떻게 두느냐

내 `memory.json`은 통짜 JSON이다. 불러오려면 전부 읽고, 검색하려면 전부 훑는다.

Hermes는 SQLite에 넣고 그 위에 전문검색 인덱스를 **두 종류** 얹었다.

- `messages_fts` — 일반 FTS5
- `messages_fts_trigram` — 3글자 단위 인덱스

트라이그램을 따로 둔 이유가 있을 것이다. 한국어처럼 띄어쓰기로 단어가
깔끔하게 안 갈리는 언어나 부분 일치 검색에서 일반 FTS5는 약하다.
15주차에 BM25를 한글 2-gram으로 직접 구현했던 것과 같은 문제의식이다.

## 2단계(이어서) — 같은 과제로 붙여봤다

`compare.py`로 동일한 출발선을 만들고 같은 목표를 줬다.
채점은 모델의 보고가 아니라 실제 디스크 상태로 했다.

> 목표: `week17/bench/*` 폴더를 정리해줘. 문서/사진/기타 폴더를 만들고
> 파일을 종류에 맞게 옮겨줘. 문서는 txt/pdf/py, 사진은 jpg/png, 나머지는 기타로.

### 붙이기까지 세 번 막혔다

**첫째, Hermes는 최소 64,000 토큰 컨텍스트를 요구한다.**

그런데 첫 실행은 통과했다. Hermes가 모델에게 컨텍스트를 물어 **131,072**를
받아 캐시했기 때문이다. 그건 모델이 *할 수 있는* 최대치다.
Ollama가 *실제로 준* 것은 기본값 **4,096**이었다.

```
Hermes가 캐시한 값 : 131,072
Ollama가 준 값     :   4,096
```

검사는 통과하고 실제로는 잘렸다. 잘리는 쪽은 맨 뒤에 붙은 사용자 메시지라,
에이전트가 "지시를 주세요"라고 영어로 답했다. **양쪽 다 에러를 내지 않는다.**
각자 자기 딴엔 정상 동작이다.

`num_ctx 16384`로 정직하게 알려주니 그제야 거부했고, 그때 64K 요구사항이 드러났다.

**둘째, 64K로 올리는 방법.** 서버 환경변수 `OLLAMA_CONTEXT_LENGTH`를 바꾸면
모든 모델에 영향이 간다. 에이전트용 태그 하나만 따로 팠다(`Modelfile.agent`).

```
FROM gemma4:e4b-it-qat
PARAMETER num_ctx 65536
```

VRAM 실측 **3.15GB, CPU로 밀린 것 0GB**. 8GB 카드에 여유 있게 들어간다.
gemma4의 PLE 구조 덕에 KV 캐시가 작다.

**셋째, config.yaml에 `model:` 키가 두 번 있었다.** 24번 줄에 내장 기본값
(`anthropic/claude-opus-4.6`), 151번 줄에 내가 추가한 Ollama 블록.
YAML은 뒤에 나온 게 이기므로 내 것이 적용됐다. 조용히 덮어쓴 것이라 알아둘 만하다.

### 3회 반복 결과

한 번 돌린 결과로 결론 내지 않는다. 15주차 judge에서 회차마다 ±1 흔들리는 것을
이미 겪었다.

| | 완주 | 회차별 점수 | 평균 시간 | 평균 도구호출 |
|---|---|---|---|---|
| **내 agent.py** | **3/3** | 7/7 · 7/7 · 7/7 | 226.9초 | 8.7 |
| Hermes | 1/3 | 0/7 · 7/7 · 0/7 | 93.8초 | 8.0 |

**내 것이 이겼다.** 2배 이상 느리지만 세 번 다 정확히 끝냈다.

### Hermes가 진 이유 — 한글을 영어로 번역했다

실패한 두 번 모두 같은 원인이었다.

```
보고서_1분기.txt → documents
사진_야유회.jpg  → photos
```

`문서`, `사진`이라고 지시했는데 `documents`, `photos` 폴더를 만들었다.
1단계에서 qwen2.5:7b가 `서울`을 `Seoul`로 넘긴 것, 12주차에 폴더가 `文档`으로
바뀐 것과 **같은 뿌리**다. 모델은 인자를 전달하는 게 아니라 생성한다.

내 agent.py에는 이 줄이 있다.

> 폴더와 파일 이름은 사용자가 말한 한국어 이름을 한 글자도 바꾸지 말고 그대로 사용한다.

12주차 사고 뒤에 넣은 문장이다. Hermes의 `SOUL.md` 513바이트에는 그런 규칙이 없다.

3단계에서 "내 프롬프트가 Hermes보다 4배 크다"는 게 부끄러운 줄 알았는데,
**그 4배가 바로 여기서 값을 했다.** 겪어서 알게 된 것이 프롬프트에 쌓여 있었다.

일반 프레임워크는 모든 언어권을 상대하니 이런 규칙을 넣을 수 없다.
**내 도메인의 실패를 아는 것은 나뿐이다.** 프레임워크가 대신해줄 수 없는 부분이 이것이다.

### Hermes가 이긴 부분 — 도구 하나의 폭

성공한 회차의 로그다.

```
💻 $ mkdir -p .../"문서" .../"사진" .../"기타"  + 7 commands   2.7s
```

터미널 도구 하나로 셸 명령 8개를 2.7초에 끝냈다. 내 에이전트가 8라운드에
걸쳐 하는 일이다.

대신 그건 **모델에게 셸을 쥐여준다**는 뜻이다. 내가 `DANGEROUS` 집합과 승인
게이트를 만든 건 정확히 그걸 피하려던 설계였다. 속도와 안전을 맞바꾼 것이고,
어느 쪽이 옳은지는 용도에 달렸다.

### MAX_ROUNDS가 잘 가던 에이전트를 잘랐다

3회 반복 전 예비 실행에서 내 에이전트가 4/7로 끝난 적이 있다.
로그를 보니 실수를 **한 번도 안 했다.**

```
라운드 1  list_files
라운드 2~4  make_folder x 3
라운드 5~8  move_file x 4      ← 여기서 MAX_ROUNDS=8 도달
```

1 + 3 + 4 = 정확히 8. 3개 남기고 상한에 걸렸다.
검증 레이어가 "미완료"를 정확히 잡아냈지만, 이미 강제 종료된 뒤였다.

3회 반복에서는 안 걸렸다. 모델이 한 라운드에 도구를 여러 개 묶어 부른
회차가 있었기 때문이다(2라운드에 7호출, 6라운드에 11호출). **모델 기분에 달렸다.**

Hermes의 `tool_loop_guardrails`는 라운드가 아니라 **실패와 무진전**을 센다.

```yaml
warn_after:      { exact_failure: 2, same_tool_failure: 3, idempotent_no_progress: 2 }
hard_stop_after: { exact_failure: 5, same_tool_failure: 8, idempotent_no_progress: 5 }
hard_stop_enabled: false
```

진도를 내는 에이전트는 아무리 오래 걸려도 안 걸린다.
**내 상한은 건강한 진행을 벌했고, 저쪽 상한은 병적인 반복을 벌한다.**

## 4단계 — 하나 훔쳐 심기

이번 주 실험이 직접 증거를 만들어준 쪽으로 정한다.

**이식 대상: 라운드 상한을 실패·무진전 기반으로 교체**

`MAX_ROUNDS = 8` 하나로 자르는 대신,
- 같은 도구·같은 인자로 반복 실패 → 센다
- 도구를 불렀는데 상태가 안 변함(무진전) → 센다
- 진도가 나가는 동안은 라운드를 세지 않는다

여전히 남는 후보: **컨텍스트 압축**(`save_memory()`가 대화를 통째로 쌓아
언젠가 터진다). 아직 안 터진 건 오래 안 써봐서다.

### 바꾼 것

```python
# 전
MAX_ROUNDS = 8
while rounds < MAX_ROUNDS: ...

# 후
WARN_AT = {"exact_failure": 2, "same_tool_failure": 3, "no_progress": 4}
STOP_AT = {"exact_failure": 5, "same_tool_failure": 8, "no_progress": 8}
HARD_CEILING = 100      # 진짜 무한루프용 최후의 벽
```

진도가 나가면 카운터를 전부 0으로 되돌린다. 이 한 줄이 라운드 상한과의
결정적 차이다 — **오래 걸려도 벌하지 않는다.**

진도는 모델의 말이 아니라 디스크로 판정한다(`world_state()`).
12주차 검증 레이어와 같은 철학이다.

경고 단계도 넣었다. 임계값에 닿으면 끊지 않고 모델에게 먼저 알려
방향을 바꿀 기회를 준다. Hermes의 `hard_stop_enabled: false`를 따라갔다.

### 결과 (5회 반복)

| | 회차별 | 완주 |
|---|---|---|
| 변경 전 (라운드 상한 8) | 7/7 7/7 7/7 | 3/3 |
| 변경 후 | 7/7 7/7 7/7 0/7 0/7 | 3/5 |

성공한 회차의 라운드 수가 **9, 12, 12**다. **예전 상한이었으면 셋 다 잘렸다.**
잘못 끊은 적은 한 번도 없다. 목적은 달성했다.

실패한 2회는 **라운드 0** — 가드레일이 실행조차 안 됐으므로 무관하다(아래).

### 도중에 걸린 것 셋

**① `move_by_extension`에 방어가 없었다**

모델이 경로에서 `bench`를 빠뜨려 `week17/mine/사진`을 넘겼는데,

```python
os.makedirs(dest, exist_ok=True)   # 없으면 그냥 만든다
```

이 줄이 그 폴더를 새로 만들어 **에러 없이** 사진 2장을 엉뚱한 데 넣었다.

`move_file`에는 12주차 사고 뒤 방어를 넣어뒀는데, 나중에 추가한 이 도구에는
안 옮겼다. **도구를 늘릴 때 방어 설계를 같이 옮기지 않으면 구멍이 생긴다.**

**② 내가 넣은 가드레일에 버그가 있었다**

```python
before = world_state(last_folder)   # 'mine'
... list_files 실행 → last_folder = 'mine/문서' 로 바뀜 ...
after  = world_state(last_folder)   # 'mine/문서'  ← 다른 폴더!
```

라운드 전후로 **서로 다른 폴더**를 비교했다. 항상 "달라졌다"가 나와서
`no_progress`가 영원히 0이었다. 감시 폴더를 목표당 한 번만 정하도록 고쳤다.

증상이 "아무 일도 안 일어남"이라 통과한 것처럼 보였다. **작동하지 않는
안전장치는 없는 것보다 나쁘다** — 있다고 믿게 되니까.

**③ Hermes 기본값을 그대로 쓰면 안 됐다**

버그를 고치니 이번엔 경고가 **정상 동작을 방해**하려 했다.

내 SYSTEM 프롬프트는 "최종 보고 전에 list_files로 다시 확인하라"고 시킨다.
목적지 폴더 3~4개를 훑는 동안 폴더는 안 변한다. 그게 정상인데
Hermes 기본값(`no_progress` 경고 2)이면 거기다 대고 "조회만 반복하지 말라"고
잔소리한다.

`no_progress`만 경고 4 / 중단 8로 늦췄다.

2단계에서는 "한글을 바꾸지 말라"를 Hermes가 **못 넣는다**고 했다.
여기서는 반대다 — Hermes의 범용 기본값이 **내 프롬프트가 시키는 검증 습관을
모른다.** 빌려온 숫자는 내 사정에 맞게 다시 재야 한다.

### 남은 문제 — 라운드 0

5회 중 2회에서 모델이 도구 호출을 **글로 썼다.**

```
봇: ```json
    { "toolName": "list_files", "arguments": {"folder": "..."} }
```

진짜 tool_call이 아니라 마크다운 코드블록이다. 그래서 라운드 0,
가드레일은 실행조차 안 됐다. 이건 다른 층의 문제라 따로 다룬다.

## 5단계 — 라운드 0의 범인은 내 프롬프트였다

`toolcall_bench.py`로 조건을 갈라 쟀다. 대화 이력은 용의선상에서 뺐다 —
`compare.py`가 매 회차 `memory.json`을 지우고 시작하기 때문이다.

| 모델 | 조건 | 도구 호출 |
|---|---|---|
| gemma4:e4b | 프롬프트 없음 | 5/5 |
| gemma4:e4b | **전체 프롬프트** | **3/5** |
| gemma4:e4b | 그 블록만 뺀 판 | 5/5 |
| qwen3.5:9b | 어느 쪽이든 | 5/5 |

범인은 12주차 사고 뒤에 **품질을 올리려고 넣은 문장**이었다.

```
도구를 호출하기 전, 매번 다음을 스스로 단계별로 따져본다:
1. ... 무엇인가
2. ... 일치하는가
3. ... 아닌가
```

gemma4는 이걸 **"글로 따져 써라"**로 읽었다. 그래서 도구 대신 계획을 서술했다.
`qwen3.5:9b`는 같은 프롬프트로도 멀쩡했다. **모델마다 다르다.**

### 지우지 않고 다시 썼다

내용은 12주차에 피 보고 얻은 규칙이라 버릴 수 없다. **틀만 바꿨다.**

```
도구를 호출할 때 지키는 것:
- 도구 결과로 확인된 사실만 쓴다. 추측하지 않는다.
- 인자는 사용자가 말한 한국어 이름과 글자 하나까지 일치시킨다.
- 직전에 에러가 난 호출을 그대로 반복하지 않는다.
계획이나 점검 과정을 글로 쓰지 말고, 바로 도구를 호출한다.
```

**질문형을 명령형으로 바꿨다.** "일치하는가?"라고 물으면 답을 쓰고,
"일치시킨다"고 하면 그냥 지킨다. 마지막 줄은 증상을 직접 금지한 것이다.

### 결과

| | 도구 호출 | 실제 과제 완주 | 평균 시간 |
|---|---|---|---|
| 원래(라운드 상한 8) | — | 3/3 | 226.9초 |
| 가드레일만 교체 | 3/5 | 3/5 | 151초 |
| **프롬프트까지 수정** | **5/5** | **5/5** | **66.2초** |

**완주 5/5, 그리고 3.4배 빨라졌다.** 서술을 안 쓰니 토큰을 안 낭비한다.

재측정에서 하나가 뒤집혔다. 새 블록을 **뺀** 판이 이번엔 3/5다.
즉 `"계획이나 점검 과정을 글로 쓰지 말고, 바로 도구를 호출한다"`
그 한 줄이 실제로 일하고 있다.

## 6단계 — Hermes는 정말 스스로 학습하는가

지금까지의 비교는 매번 새 세션으로 단발 과제를 반복한 것이라,
홍보 문구가 말하는 장점(스킬 자동 생성, 세션 초월 메모리)을 **아예 측정하지
못했다.** 축을 바꿔 다시 쟀다(`accumulate.py`).

`--continue`로 같은 세션을 이어가며 비슷한 일을 6번 시켰다.
2회차부터는 규칙을 알려주지 않고 "아까와 같은 방식으로"라고만 했다.

| 회차 | 시간 | 도구 | 점수 | 새 스킬 | 새 메모리 |
|---|---|---|---|---|---|
| 1 | 72.0초 | 6 | **7/7** | 0 | 0 |
| 2 | 38.9초 | 8 | 0/7 | 0 | 0 |
| 3 | 26.5초 | 10 | 0/7 | 0 | 0 |
| 4 | 32.3초 | 12 | 0/7 | 0 | 0 |
| 5 | 26.8초 | 14 | 0/7 | 0 | 0 |
| 6 | 31.9초 | 16 | 0/7 | 0 | 0 |

```
curator-managed skills: 70 total  (agent-created=0  bundled=70)
runs: 0    last run: never    interval: every 7d
```

**스킬 0개, 메모리 0개.** 546개 파일 / 70개 스킬은 전부 설치 때 깔린 번들이고,
curator가 직접 세어준 `agent-created=0`이 그걸 확인해준다.
그리고 curator는 **7일 주기**라 아직 한 번도 안 돌았다.

더 나쁜 건 **2회차부터 전부 0/7**이라는 점이다. 도구 호출은 6→16으로 늘었는데
결과가 없다. "아까와 같은 방식"을 살려내지 못했다.

### 다만 공정하게

이 결과에는 내 실험 설계 탓도 있다.

- **6회는 짧다.** curator가 7일 주기면 며칠~몇 주 써야 보이는 기능이다
- **모델이 약하다.** gemma4:e4b가 긴 세션에서 무너지는 건 5단계에서 이미 봤다
- **과제가 단순하다.** "폴더 정리"가 스킬로 만들 만한 노하우가 아닐 수 있다

정확한 결론은 **"기능이 거짓"이 아니라 "이 조건에서는 발동하지 않는다"**다.

### 기능 주장 대조표 (실물 확인)

| 주장 | 코드에 있나 | 우리 환경에서 일어났나 |
|---|---|---|
| 스킬 자동 생성 | 있음 | **0건** |
| 세션 초월 메모리 | 있음 | `state.db`엔 쌓임 / `memories/`는 **0개** |
| 모델 독립성 | 있음 | **확인됨** (Ollama 연동) |
| 멀티채널 | 있음 | 안 써봄 |
| 크론 자동화 | 있음 | `cron/` 0개 |

## 7단계 — "자기 학습"은 사실이었다 (내가 네 번 틀렸다)

6단계에서 "자기 학습은 광고만큼 안 된다"고 썼다. **틀렸다.**
학생이 계속 밀어붙여서 네 번 다시 팠고, 네 번 다 내 실험이 문제였다.

| 내 주장 | 실제 |
|---|---|
| "6턴이라 넛지 주기 미달" | 24턴 돌려도 0 — **턴 축 자체가 틀렸음** |
| "gemma4가 약해서" | GPT-4.1도 0 — **모델 문제 아님** |
| "-q 모드에서 데몬 스레드가 죽어서" | 기다려줘도 0 — **부분 원인일 뿐** |
| "자동으로는 안 된다" | **교정을 주니 됐다** |

### 소스를 읽고 알아낸 것

```python
# turn_finalizer.py — 넛지는 "이번 실행에서 쓴 도구 반복 횟수" 기준
if (agent._skill_nudge_interval > 0
        and agent._iters_since_skill >= agent._skill_nudge_interval ...):

# run_agent.py — 리뷰는 데몬 스레드
t = threading.Thread(target=..., daemon=True, name="bg-review")
```

`agent_init.py`가 프로세스마다 카운터를 0으로 초기화한다. 즉 내가 센
"user 턴 24회"와 실제로 세는 "한 실행 안의 도구 반복 15회"는 **다른 축**이었다.
그리고 데몬 스레드는 `-q` 모드에서 응답 직후 프로세스가 죽으며 잘린다.

### 결정적이었던 것 — 리뷰 프롬프트

```
"Be ACTIVE — most sessions produce at least one skill update, even if small.
 A pass that does nothing is a missed learning opportunity."

신호:
  · 사용자가 말투·형식·장황함을 지적 (FIRST-CLASS signal)
  · 사용자가 작업 방식·순서를 교정
  · 사소하지 않은 기법·우회로가 나옴
  · 불러 쓴 스킬이 틀렸거나 빠짐
```

**전부 "사용자와 부딪히며 배우는" 상황이다.**

그런데 나는 규칙을 다 알려주고 한 번에 되는 일만 시켰다.
지적도 교정도 실패도 없었다. **배울 게 없으니 안 만든 게 옳은 판단이었다.**

### 마찰을 주니 나왔다

일부러 틀리게 시키고, 지적하고, 말투를 교정하는 7턴 세션을 짰다.
매 턴 `bg-review`가 완주하도록 atexit로 붙잡았다.

```
[4/7] 스킬+1   skills/software-development/file-organization-policy/SKILL.md
[6/7] 메모리+2

최종:  learned_skills: 1  /  agent_created: 1  /  memory_nodes: 6
```

**자동 생성된 스킬** (class-level 이름, 세션 한 번짜리 이름이 아니다)

```markdown
name: file-organization-policy

# 규칙 및 절차
1. ls로 현재 상태 확인(반드시 실행)
2. 폴더 생성(이름 변형 금지)
3. 파일 이동 ... 문서: .pdf, .txt, .md, .docx, .xlsx, .py
4. ls로 결과 재확인(이동 후 필수)
5. 보고: 결과만 한 줄로 간결하게

# Pitfalls
- 중간 확인 없이 이동 금지
- 이름 변경·추가 변형 금지
```

**자동 생성된 사용자 프로파일** (`USER.md`)

> 사용자는 파일 정리에 대해 단계별 절차와, 결과는 반드시 한 줄로 간결하게
> 보고할 것을 기대하고 시정 요구했다. 설명, 과정을 나열하지 않는 최소 결과 전달 선호.

**"기억해둬"라고 시킨 적이 없다.** 교정만 했는데 규칙으로 뽑아 저장했다.

### 발동 조건 (확정)

```
① 사용자의 교정·지적·마찰이 대화에 있을 것        ← 제일 중요
② 한 실행 안에서 도구 반복이 문턱 이상 (기본 15)
③ bg-review 데몬 스레드가 완주할 것               ← -q 단발 모드의 함정
```

③은 실제 대화형으로 쓰면 프로세스가 살아있어 자연히 해결된다.
스크립트로 자동화해서 생긴 문제였다.

### 스킬과 메모리의 분업

| | 메모리 `MEMORY.md` | 스킬 `SKILL.md` |
|---|---|---|
| 내용 | 사실 (".py는 문서로") | 절차 ("①확인 ②생성 ③이동 ④재확인") |
| 주입 | 항상 | 트리거가 맞을 때만 |
| 크기 | 문자 수 제한 | 길어도 됨, 파일 여러 개 가능 |

메모리를 무한정 늘리면 매 요청 토큰을 먹으니, 긴 절차는 스킬로 빼서
필요할 때만 꺼내는 구조다.

## 이번 주 결론

```
프레임워크가 확실히 주는 것 : 배관 (가드레일·파서·압축·기억 인프라·채널·크론)
프레임워크가 못 주는 것     : 내 도메인 지식 — 넣어줘야 들어간다
진짜인 것                  : 자기 학습. 단 "부딪혀야" 배운다
```

| | 헤르메스 | 클로드 코드 |
|---|---|---|
| 알아서 기억 | ✅ | ✅ |
| **알아서 스킬 생성** | **✅** | ❌ |
| 사용자 프로파일 자동 생성 | ✅ `USER.md` | △ |
| 로컬 모델 | ✅ | ❌ |

**스킬 자동 생성은 헤르메스만 된다.** 그게 이 도구를 쓸 이유다.

### 배운 것 중 제일 큰 것

측정이 0으로 나왔을 때 **"기능이 없다"가 아니라 "내가 발동 조건을 못 맞췄다"**를
먼저 의심해야 했다. 나는 네 번 연속 전자로 결론 냈고 네 번 다 틀렸다.

15주차에 "**점수판 자체가 먼저 틀렸다**"를 배웠는데(첫 76%는 채점 결함, 실제 90%),
같은 실수를 규모만 키워 반복했다. 그때는 채점기가 틀렸고, 이번엔 실험 설계가 틀렸다.

소스가 손에 있었는데도 4단계까지 안 읽고 추측만 했다. **오픈소스를 쓰는 값어치는
막혔을 때 안을 열어볼 수 있다는 것**인데, 그걸 늦게 썼다.

그런데 **이번 주 가장 큰 소득은 프레임워크 쪽이 아니었다.**

남의 것과 붙여보려고 조건을 통제해 재는 과정에서, **내 프롬프트가 내 에이전트의
도구 호출을 망가뜨리고 있었다**는 걸 찾았다. 완주 3/5 → 5/5, 속도 3.4배.

비교의 값어치는 남의 것을 베끼는 데 있는 게 아니라, **내 것을 제대로 재보게
만드는 데** 있었다.
